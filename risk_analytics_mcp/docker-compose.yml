version: "3.9"

services:
  risk-analytics-mcp:
    build:
      context: ..
      dockerfile: risk_analytics_mcp/Dockerfile
    env_file:
      - .env
    ports:
      - "${RISK_MCP_PORT:-8010}:8010"
    restart: unless-stopped
